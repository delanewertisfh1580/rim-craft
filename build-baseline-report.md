# RimWorldCraft build baseline report

## Scope

This report records the Gradle stabilization pass for the current Java 17 JVM skeleton. Existing user changes were preserved. No Java source, `.env` file, Forge dependency, Minecraft runtime, Baritone coordinate, credential, or external environment was changed.

## Wrapper inventory

| Item | Result |
|---|---|
| `gradlew` | Present and executable after wrapper generation (`chmod +x`) |
| `gradlew.bat` | Present |
| `gradle/wrapper/gradle-wrapper.properties` | Present; configured for Gradle `8.10.2` binary distribution |
| `gradle/wrapper/gradle-wrapper.jar` | Present; generated by Gradle 8.10.2 |
| Standard wrapper generation | DONE with Gradle 8.10.2 |

A wrapper JAR was intentionally not fabricated. The launcher reports the missing JAR rather than pretending to be a standard generated wrapper.

## Environment checks

Commands run from the repository root:

```text
java -version
openjdk version "17.0.20" 2026-07-21

.tooling/gradle-8.10.2/bin/gradle wrapper --gradle-version 8.10.2 --no-daemon
BUILD SUCCESSFUL

./gradlew --version
Gradle 8.10.2

./gradlew clean build --no-daemon
BUILD SUCCESSFUL
```

The build completed for all three active modules. It emitted existing compiler and Gradle deprecation warnings, but no compilation or test failures.

## Active modules

`settings.gradle` currently includes only:

- `core-api`
- `core-impl`
- `infrastructure-common`

`infrastructure-forge`, `test-common`, `test-core`, and `test-integration` remain excluded because Forge/Minecraft wiring and their required build setup are not present in the active JVM skeleton.

## Source consistency scan

The repository scan found no additional public top-level declarations requiring a file split beyond the already-normalized legacy facades. `Repositories.java` and `CoreServices.java` now contain package-private compatibility declarations, while `ForgeAdapters.java` contains one public marker class. No Forge or Baritone coordinates were added.

Known API migration risks remain in legacy source (for example raw UUID and compatibility `Position` usage); resolving those belongs to the Java architecture migration and was not expanded into an unrelated rewrite during this build-only pass.

## Status

**DONE** — Java 17 and Gradle 8.10.2 were installed in the sandbox, the standard wrapper was generated, `gradlew` was made executable, and the requested build passed. The next verification should address the warnings and run the focused test/quality gates as they are added:

```bash
./gradlew clean build --no-daemon
```

No commit, push, reset, clean, or history rewrite was performed.
